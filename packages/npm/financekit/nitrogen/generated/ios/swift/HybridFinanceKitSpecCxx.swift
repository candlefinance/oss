///
/// HybridFinanceKitSpecCxx.swift
/// Sat Aug 31 2024
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

import Foundation
import NitroModules

/**
 * A class implementation that bridges HybridFinanceKitSpec over to C++.
 * In C++, we cannot use Swift protocols - so we need to wrap it in a class to make it strongly defined.
 *
 * Also, some Swift types need to be bridged with special handling:
 * - Enums need to be wrapped in Structs, otherwise they cannot be accessed bi-directionally (Swift bug: https://github.com/swiftlang/swift/issues/75330)
 * - Other HybridObjects need to be wrapped/unwrapped from the Swift TCxx wrapper
 * - Throwing methods need to be wrapped with a Result<T, Error> type, as exceptions cannot be propagated to C++
 */
public final class HybridFinanceKitSpecCxx {
  /**
   * The Swift <> C++ bridge's namespace (`margelo::nitro::financekit::bridge::swift`)
   * from `candlefinance_financekit-Swift-Cxx-Bridge.hpp`.
   * This contains specialized C++ templates, and C++ helper functions that can be accessed from Swift.
   */
  public typealias bridge = margelo.nitro.financekit.bridge.swift

  /**
   * Holds an instance of the `HybridFinanceKitSpec` Swift protocol.
   */
  private(set) var implementation: HybridFinanceKitSpec

  /**
   * Create a new `HybridFinanceKitSpecCxx` that wraps the given `HybridFinanceKitSpec`.
   * All properties and methods bridge to C++ types.
   */
  public init(_ implementation: HybridFinanceKitSpec) {
    self.implementation = implementation
  }

  /**
   * Contains a (weak) reference to the C++ HybridObject to cache it.
   */
  public var hybridContext: margelo.nitro.HybridContext {
    get {
      return self.implementation.hybridContext
    }
    set {
      self.implementation.hybridContext = newValue
    }
  }

  /**
   * Get the memory size of the Swift class (plus size of any other allocations)
   * so the JS VM can properly track it and garbage-collect the JS object if needed.
   */
  public var memorySize: Int {
    return self.implementation.memorySize
  }

  // Properties
  

  // Methods
  @inline(__always)
  public func requestAuthorization() -> bridge.PromiseHolder_AuthorizationStatus_ {
    do {
      let result = try self.implementation.requestAuthorization()
      return { () -> bridge.PromiseHolder_AuthorizationStatus_ in
        let promiseHolder = bridge.create_PromiseHolder_AuthorizationStatus_()
        result
              .then({ promiseHolder.resolve(margelo.nitro.financekit.AuthorizationStatus(rawValue: $0.rawValue) ?? .notdetermined) })
          .catch({ promiseHolder.reject(std.string(String(describing: $0))) })
        return promiseHolder
      }()
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
  
  @inline(__always)
  public func authorizationStatus() -> bridge.PromiseHolder_AuthorizationStatus_ {
    do {
      let result = try self.implementation.authorizationStatus()
      return { () -> bridge.PromiseHolder_AuthorizationStatus_ in
        let promiseHolder = bridge.create_PromiseHolder_AuthorizationStatus_()
        result
              .then({ promiseHolder.resolve(margelo.nitro.financekit.AuthorizationStatus(rawValue: $0.rawValue) ?? .notdetermined) })
          .catch({ promiseHolder.reject(std.string(String(describing: $0))) })
        return promiseHolder
      }()
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
  
  @inline(__always)
  public func transactions(query: Query) -> bridge.PromiseHolder_std__vector_FinanceKitTransaction__ {
    do {
      let result = try self.implementation.transactions(query: query)
      return { () -> bridge.PromiseHolder_std__vector_FinanceKitTransaction__ in
        let promiseHolder = bridge.create_PromiseHolder_std__vector_FinanceKitTransaction__()
        result
          .then({ r in promiseHolder.resolve({ () -> bridge.std__vector_FinanceKitTransaction_ in
        var vector = bridge.create_std__vector_FinanceKitTransaction_(r.count)
        for item in r {
          vector.push_back(item)
        }
        return vector
      }()) })
          .catch({ promiseHolder.reject(std.string(String(describing: $0))) })
        return promiseHolder
      }()
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
  
  @inline(__always)
  public func transactionHistory(params: AccountDetailsHistoryParams) -> bridge.PromiseHolder_std__vector_FinanceKitTransaction__ {
    do {
      let result = try self.implementation.transactionHistory(params: params)
      return { () -> bridge.PromiseHolder_std__vector_FinanceKitTransaction__ in
        let promiseHolder = bridge.create_PromiseHolder_std__vector_FinanceKitTransaction__()
        result
          .then({ r in promiseHolder.resolve({ () -> bridge.std__vector_FinanceKitTransaction_ in
        var vector = bridge.create_std__vector_FinanceKitTransaction_(r.count)
        for item in r {
          vector.push_back(item)
        }
        return vector
      }()) })
          .catch({ promiseHolder.reject(std.string(String(describing: $0))) })
        return promiseHolder
      }()
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
  
  @inline(__always)
  public func accounts(query: Query) -> bridge.PromiseHolder_std__vector_Account__ {
    do {
      let result = try self.implementation.accounts(query: query)
      return { () -> bridge.PromiseHolder_std__vector_Account__ in
        let promiseHolder = bridge.create_PromiseHolder_std__vector_Account__()
        result
          .then({ r in promiseHolder.resolve({ () -> bridge.std__vector_Account_ in
        var vector = bridge.create_std__vector_Account_(r.count)
        for item in r {
          vector.push_back(item)
        }
        return vector
      }()) })
          .catch({ promiseHolder.reject(std.string(String(describing: $0))) })
        return promiseHolder
      }()
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
  
  @inline(__always)
  public func accountHistory(params: AccountHistoryParams) -> bridge.PromiseHolder_std__vector_Account__ {
    do {
      let result = try self.implementation.accountHistory(params: params)
      return { () -> bridge.PromiseHolder_std__vector_Account__ in
        let promiseHolder = bridge.create_PromiseHolder_std__vector_Account__()
        result
          .then({ r in promiseHolder.resolve({ () -> bridge.std__vector_Account_ in
        var vector = bridge.create_std__vector_Account_(r.count)
        for item in r {
          vector.push_back(item)
        }
        return vector
      }()) })
          .catch({ promiseHolder.reject(std.string(String(describing: $0))) })
        return promiseHolder
      }()
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
  
  @inline(__always)
  public func accountBalances(query: Query) -> bridge.PromiseHolder_std__vector_FinanceKitAccountBalance__ {
    do {
      let result = try self.implementation.accountBalances(query: query)
      return { () -> bridge.PromiseHolder_std__vector_FinanceKitAccountBalance__ in
        let promiseHolder = bridge.create_PromiseHolder_std__vector_FinanceKitAccountBalance__()
        result
          .then({ r in promiseHolder.resolve({ () -> bridge.std__vector_FinanceKitAccountBalance_ in
        var vector = bridge.create_std__vector_FinanceKitAccountBalance_(r.count)
        for item in r {
          vector.push_back(item)
        }
        return vector
      }()) })
          .catch({ promiseHolder.reject(std.string(String(describing: $0))) })
        return promiseHolder
      }()
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
  
  @inline(__always)
  public func accountBalanceHistory(params: AccountDetailsHistoryParams) -> bridge.PromiseHolder_std__vector_FinanceKitAccountBalance__ {
    do {
      let result = try self.implementation.accountBalanceHistory(params: params)
      return { () -> bridge.PromiseHolder_std__vector_FinanceKitAccountBalance__ in
        let promiseHolder = bridge.create_PromiseHolder_std__vector_FinanceKitAccountBalance__()
        result
          .then({ r in promiseHolder.resolve({ () -> bridge.std__vector_FinanceKitAccountBalance_ in
        var vector = bridge.create_std__vector_FinanceKitAccountBalance_(r.count)
        for item in r {
          vector.push_back(item)
        }
        return vector
      }()) })
          .catch({ promiseHolder.reject(std.string(String(describing: $0))) })
        return promiseHolder
      }()
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
}
